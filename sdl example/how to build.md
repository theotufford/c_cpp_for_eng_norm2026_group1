# how to build our sdl2 project
If you havent built it ever, ensure you have cmake and sdl2 installed.
then, within the "build" directory run: `cmake ..`. Now, any time you want
to run/ test the program, ensure that you are in the build folder and run `make`.
